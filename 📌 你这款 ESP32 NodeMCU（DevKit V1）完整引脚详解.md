# 📌 你这款 ESP32 NodeMCU（DevKit V1）完整引脚详解

你图片里的是**最通用经典款 ESP32 DevKit V1（NodeMCU\-32S）开发板**，板载 WIFI \+ 蓝牙，Micro USB 接口，两边排针从上到下完整引脚、复用功能、使用禁忌全部给你列全，同时结合你之前 OV7670 摄像头代码里的引脚做重点标注。

## 一、引脚总图预览

## 二、分左右两边完整引脚清单（**正对开发板，USB 口朝下**）

### 🔹 开发板【左侧排针】（从上 → 从下）

|引脚编号|核心复用功能|重点备注 \&amp; 使用禁忌|
|---|---|---|
|`36 \(VP\)`|ADC1\_CH0、仅**输入**引脚|无法输出高低电平，仅读取模拟信号|
|`39 \(VN\)`|ADC1\_CH3、仅**输入**引脚|无法输出高低电平，仅读取模拟信号|
|`34`|ADC1\_CH6、仅**输入**引脚|无法输出高低电平，仅读取模拟信号|
|`35`|ADC1\_CH7、仅**输入**引脚|无法输出高低电平，仅读取模拟信号|
|`32`|ADC1\_CH4、触摸引脚 T9、I2C/SPI|通用 IO，可输入输出，模拟采集|
|`33`|ADC1\_CH5、触摸引脚 T8、I2C/SPI|通用 IO，可输入输出，模拟采集|
|`25`|DAC1、ADC2\_CH8|通用 IO，自带数模转换 DAC 输出|
|`26`|DAC2、ADC2\_CH9|通用 IO，自带数模转换 DAC 输出|
|`27`|ADC2\_CH7、触摸引脚 T7|通用 IO，可输入输出|
|`14`|ADC2\_CH6、触摸引脚 T6、SPI 时钟|通用 IO，外设常用引脚|
|`12`|ADC2\_CH5、触摸引脚 T5、启动引导脚|⚠️**高危锁死引脚**！上电电平会决定启动模式，尽量不要外接设备|
|`13`|ADC2\_CH4、触摸引脚 T4、SPI|通用 IO，非常稳定好用的通用引脚|
|`15`|ADC2\_CH3、触摸引脚 T3、启动引导脚|上电必须拉高，外设 CS 片选常用引脚|
|`2`|ADC2\_CH2、触摸引脚 T2、启动引导脚|板载 LED 绑定引脚，通用 IO|
|`4`|ADC2\_CH1、触摸引脚 T0|通用 IO，**你代码里 OV7670 常用备用引脚**|

---

### 🔹 开发板【右侧排针】（从上 → 从下）

|引脚编号|核心复用功能|重点备注 \&amp; 使用禁忌|
|---|---|---|
|`5`|SPI\_CS、通用 IO|稳定通用引脚，外设常用|
|`18`|SPI\_SCK、通用 IO|SPI 时钟引脚，通用 IO|
|`19`|SPI\_MISO、通用 IO|SPI 数据引脚，通用 IO|
|`21`|I2C SDA|✅ **你代码里定义的摄像头 I2C 数据线 ****`SIOD\_PIN 21`**|
|`22`|I2C SCL|✅ **你代码里定义的摄像头 I2C 时钟线 ****`SCLK\_PIN 22`**|
|`23`|SPI\_MOSI、通用 IO|SPI 数据引脚，通用 IO|
|`TX0 \(GPIO1\)`|串口 0 发送 TX|Arduino 上传串口，**尽量不要外接**|
|`RX0 \(GPIO3\)`|串口 0 接收 RX|Arduino 上传串口，**尽量不要外接**|
|`BOOT \(GPIO0\)`|下载启动脚|刷机按键引脚，下载模式控制脚，尽量不用做普通 IO|
|`16`|UART2、通用 IO|稳定通用 IO，串口外设常用|
|`17`|UART2、通用 IO|稳定通用 IO，串口外设常用|

---

### 🔹 电源引脚（四角 \&amp; 上下固定引脚）

1. **3\.3V**：开发板稳压输出 3\.3V，给外设供电（**最大输出电流很小，不能驱动大功率模块**）

2. **5V**：USB 进来的 5V 电源引脚

3. **GND**：接地引脚，开发板左右、上下都有 GND，电路必须共地

4. **EN**：使能引脚，接高电平开发板开机，悬空默认开机

## 三、结合你之前 OV7670 摄像头代码重点标注

你截图里的代码：

```cpp
#define SIOD_PIN 21  // I2C SDA
#define SCLK_PIN 22  // I2C SCL
```

正好对应开发板**右侧排针的原生硬件 I2C 引脚**：

- `GPIO21` = ESP32 硬件 I2C 默认 SDA

- `GPIO22` = ESP32 硬件 I2C 默认 SCL
这组引脚是 ESP32 官方硬件 I2C 专用引脚，驱动 OV7670 屏幕、OLED 屏幕都用这组，兼容性最好，不用软件模拟 I2C。

## 四、新手必看避坑红线⚠️

1. **只读引脚（只能读、不能输出）**：`GPIO34、35、36、39`
这 4 个引脚**没有输出驱动能力**！只能接传感器读信号，不能用来点灯、驱动舵机、模块高低电平控制。

2. **启动引导危险引脚**：`GPIO0、GPIO2、GPIO4、GPIO12、GPIO15`
上电瞬间的电平会决定 ESP32 能不能正常开机，**新手尽量不要乱接**，尤其是`GPIO12`，接错直接芯片砖化无法刷机。

3. **串口下载引脚**：`GPIO1\(TX\)、GPIO3\(RX\)`
Arduino IDE 上传程序、串口监视器打印信息全靠这两个脚，外接设备会导致上传失败、串口乱码。

4. **ESP32 全部 IO 都是 3\.3V 电平！绝对不能接 5V 外设**
直接接 5V 会烧坏芯片 IO 口！外设 5V 必须加电平转换。

5. 稳定万能备用引脚推荐（新手放心随便用）：
`GPIO5、13、16、17、18、19、23、25、26、27、32、33`

## 五、完整色彩功能速查图

需要我给你整理一份**OV7670 摄像头专用安全引脚接线表**，把电源、D0\~D7、像素时钟、复位、I2C 全部对应好 ESP32 引脚，直接照着接线就行么？

> （注：文档部分内容可能由 AI 生成）
